<ContentDialog
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:USCISCaseTracker.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Models="using:USCISCaseTracker.Models"
    x:Class="USCISCaseTracker.UWP.AddCaseDialog"
    mc:Ignorable="d"
    Title="Add case"
    PrimaryButtonText="Add"
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <ContentDialog.DataContext>
        <Models:Case/>
    </ContentDialog.DataContext>

    <StackPanel>
        <TextBox Text="{Binding Name, Mode=TwoWay}" Header="Name" />
        <TextBox Text="{Binding ReceiptNumber, Mode=TwoWay}" Header="Receipt number"/>
    </StackPanel>
</ContentDialog>
